# Create a record (a 'dictionary') called 'info' to store facts.
info = {
    "name":(input("What is your name?:")),
    # Ask the user where they live and save it under "hometown"
    "hometown":(input("Where do you live?:")),
    # Ask the user their age and save it under "age"
    "age": (input("How old are you?:"))
}
# The program saves three answers in one list.